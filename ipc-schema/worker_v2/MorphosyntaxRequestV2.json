{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "MorphosyntaxRequestV2",
  "description": "V2 morphosyntax request payload.",
  "type": "object",
  "required": [
    "item_count",
    "lang",
    "payload_ref_id"
  ],
  "properties": {
    "item_count": {
      "description": "Number of utterance items frozen into the prepared batch payload.",
      "type": "integer",
      "format": "uint32",
      "minimum": 0.0
    },
    "lang": {
      "description": "Primary language routed by Rust.",
      "type": "string"
    },
    "payload_ref_id": {
      "description": "Reference to the prepared text batch payload.",
      "type": "string"
    },
    "retokenize": {
      "description": "Whether Stanza/PyCantonese should re-tokenize the input.\n\nWhen `true`, CJK word segmentation is applied before POS tagging: - Cantonese (`yue`): PyCantonese `segment()` groups characters into words - Mandarin (`cmn`/`zho`): Stanza neural tokenizer segments the text\n\nDefaults to `false` for backward compatibility with workers that do not yet understand this field.",
      "default": false,
      "type": "boolean"
    }
  }
}
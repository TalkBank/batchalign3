{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ChainRef",
  "description": "A single coreference chain reference on a word.\n\nRepresents one chain that a word participates in. A word can simultaneously start one chain and end another.\n\n# Bracket notation mapping\n\n| is_start | is_end | notation | |----------|--------|----------| | true     | false  | `(N`     | | false    | true   | `N)`     | | true     | true   | `(N)`    | | false    | false  | `N`      |",
  "type": "object",
  "required": [
    "chain_id",
    "is_end",
    "is_start"
  ],
  "properties": {
    "chain_id": {
      "description": "Chain identifier (0-based, assigned by Stanza).",
      "type": "integer",
      "format": "uint",
      "minimum": 0.0
    },
    "is_end": {
      "description": "Whether this word ends a mention in this chain.",
      "type": "boolean"
    },
    "is_start": {
      "description": "Whether this word starts a mention in this chain.",
      "type": "boolean"
    }
  }
}